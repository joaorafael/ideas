@using WebIdeas.Models
@{
    var tags = ((ITags) @Model).Tags;
}
<div id="categorias">
    <ul>
        @foreach(var tag in tags)
        {
            <li><a href="@Url.Action(@tag.Id.ToString(), "Category")">@tag.Name</a></li>
        }
    </ul>
</div>